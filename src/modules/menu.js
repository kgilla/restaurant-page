import {makeElement} from './functions';

const renderMenu = () => {

  const page = document.querySelector(".page");
  
  const heading = makeElement("h1", "heading", "Suck My Dick", page);
};

export {renderMenu};